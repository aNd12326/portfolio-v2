<div class="py-[240px] flex items-center justify-center mx-auto">
    <div class="w-[1220px] grid grid-cols-2 gap-10">
        <div class="flex flex-col">
            <h2 class="text-5xl w-1/2">
                Interested in working together? Let's talk ➡️
            </h2>
        </div>

        <div class="flex flex-col gap-y-20">
            <input
                type="text"
                id="name"
                name="name"
                required
                size="10"
                placeholder="Enter your name"
                class="pb-8 border-b border-gray-400 bg-transparent focus:outline-none text-xl focus:border-gray-950 transition ease-in-out duration-500"
            />

            <input
                type="email"
                id="email"
                name="email"
                required
                size="10"
                placeholder="Your email adress"
                class="pb-8 border-b border-gray-400 bg-transparent focus:outline-none text-xl focus:border-gray-950 transition ease-in-out duration-500"
            />

            <textarea
                placeholder="Describe your project"
                maxlength="500"
                class="pb-8 border-b border-gray-400 bg-transparent focus:outline-none text-xl focus:border-gray-950 transition ease-in-out duration-500"
            ></textarea>

            <div class="items-start">
                <input
                    type="submit"
                    id="btnsubmit"
                    name="btnsubmit"
                    class="pb-2 cursor-pointer font-extrabold text-xl border-b border-gray-400 bg-transparent focus:outline-none hover:border-gray-950 transition ease-in-out duration-500"
                    value="Contact me ➡️"
                />
            </div>
        </div>
    </div>
</div>

<style>
    input::placeholder {
        font-weight: 600;
        color: black;
    }

    textarea::placeholder{
        font-weight: 600;
        color: black;
    }
</style>
